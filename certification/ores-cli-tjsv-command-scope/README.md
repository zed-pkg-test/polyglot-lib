# ORES CLI TJSV command-scope certificate

This public `zed-pkg-test` certificate binds `ORESoftware/ores-cli#244` at semantic current-main head `cca1e76e79a1d233ea7dfd1050abb0ebe9a0d049`.

The exact mirrored invocation-scope module rejects false peer-authority admission assembled across files, workflow steps, or separate shell commands. A complete immutable TJSV action or one logical `tjsv check` command must name both independently authored authorities plus the parity-report and generated-Schema-B destinations.

TypeSpec and authored JSON Schema Draft 2020-12 remain equal first-class authorities with precedence `none`. Generated Schema B is comparison evidence only and is compared with independently authored Schema A.

The source lock also binds the current product dispatcher so the certificate proves composition with OAuth/OIDC linting, generated-Schema provenance checks, full TJSV admission, revision consistency, and the runtime env-boundary audit.
