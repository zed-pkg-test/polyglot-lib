[project]
name = "zedtest-polyglot-lib"
version = "0.1.0"
description = "Python slice of the polyglot test library"
license = { text = "MIT" }
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
